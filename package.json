{
  "name": "circuit_breaker",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "repository": "https://github.com/ShapeProject/circuit_breaker.git",
  "author": "Haruki Kondo",
  "license": "MIT",
  "scripts": {
    "backend:build": "yarn workspace backend compile",
    "backend:test": "yarn workspace backend test",
    "backend:cp:contracts": "yarn workspace backend cp:contracts",
    "backend:deploy:scrollSepolia": "yarn workspace backend deploy:scrollSepolia",
    "backend:verify:scrollSepolia": "yarn workspace backend verify:scrollSepolia",
    "backend:deploy:mock:scrollSepolia": "yarn workspace backend deploy:mock:scrollSepolia",
    "backend:script:verify:mock:scrollSepolia": "yarn workspace backend script:verify:mock:scrollSepolia",
    "backend:script:gaslessVerify:mock:scrollSepolia": "yarn workspace backend script:gaslessVerify:mock:scrollSepolia",
    "backend:script:gaslessSetScore:mock:scrollSepolia": "yarn workspace backend script:gaslessSetScore:mock:scrollSepolia",
    "circuit:test": "yarn workspace circuit test",
    "circuit:executeGroth16": "yarn workspace circuit executeGroth16",
    "circuit:cp:verifier": "yarn workspace circuit cp:verifier",
    "frontend:build": "yarn workspace frontend build",
    "frontend:dev": "yarn workspace frontend dev",
    "frontend-sandbox:build": "yarn workspace frontend-sandbox build",
    "frontend-sandbox:dev": "yarn workspace frontend-sandbox dev",
    "scripts:sample": "yarn workspace scripts sample"
  },
  "workspaces": [
    "pkgs/*"
  ],
  "devDependencies": {
    "@types/node": "^20.11.18",
    "@types/react": "^18.2.55"
  }
}